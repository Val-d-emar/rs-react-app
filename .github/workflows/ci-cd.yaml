# .github/workflows/ci-cd.yml
name: CI/CD Pipeline

# Триггеры: запускать воркфлоу при push или pull_request в ветку 'main', 'dev'
on:
  push:
    branches: ['main', 'dev']
  pull_request:
    branches: ['main', 'dev']
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

permissions:
  contents: read # Право на чтение содержимого репозитория (для checkout)
  pull-requests: write # Право на запись в Pull Requests (для комментирования)

jobs:
  build-and-test:
    # Запускать на последней версии Ubuntu
    runs-on: ubuntu-latest

    steps:
      # Шаг 1: Клонирование вашего репозитория
      - name: Checkout repository
        uses: actions/checkout@v4

      # Шаг 2: Настройка Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      # Шаг 3: Установка зависимостей
      - name: Install dependencies
        run: npm i
      # ... после шага "Install dependencies"

      # Шаг 4: Проверка линтером и форматом
      - name: Run linter and check format
        run: |
          npm run lint
          npm run format:check

      # Шаг 5: Запуск тестов
      - name: Run tests
        run: npm run test -- --run # Флаг --run для Vitest, чтобы он завершился после тестов

      # Шаг 5.1: Генерация отчета о покрытии
      - name: Run tests and generate coverage summary
        id: coverage
        env:
          NO_COLOR: true # Отключаем цветной вывод для лучшей читаемости в комментариях
        # Запуск тестов с покрытием и сохранение результатов в файл
        run: npm run test:coverage -- --run | tee coverage-summary.txt
      # Шаг 5.2: Удаление старого комментария с покрытием в PR
      - name: Delete old coverage comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const botCommentIdentifier = "## 🧪 Test Coverage Report";

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const oldComment = comments.find(comment => 
              comment.user.login === 'github-actions[bot]' && 
              comment.body.startsWith(botCommentIdentifier)
            );

            if (oldComment) {
              console.log(`Found old comment with id: ${oldComment.id}. Deleting...`);
              await github.rest.issues.deleteComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: oldComment.id,
              });
              console.log('Old comment deleted.');
            } else {
              console.log('No old coverage comment found.');
            }
      # Шаг 5.3: Публикация комментария с результатами в PR
      - name: Post coverage summary to PR
        # Запускать только для событий pull_request
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          # Этот токен предоставляется GitHub автоматически и имеет права на комментирование
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const summary = fs.readFileSync('coverage-summary.txt', 'utf8');

            const body = `## 🧪 Test Coverage Report\n\n\`\`\`\n${summary}\n\`\`\``;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: body
            });
      # 3. Шаг для загрузки полного HTML-отчета как артефакта
      - name: Upload full coverage report as artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-html
          path: coverage/
          retention-days: 7 # Хранить артефакт 7 дней
      # Шаг 6: Сборка Docker-образа для теста
      - name: Build Docker image for testing
        if: github.ref == 'refs/heads/dev' && github.event_name != 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true # Загружает образ в локальный Docker, чтобы его можно было запустить
          tags: test-image:latest

      # Шаг 7: Тестирование запущенного контейнера
      - name: Test running container
        if: github.ref == 'refs/heads/dev' && github.event_name != 'pull_request'
        run: |
          docker run -d --name test-container -p 8080:80 test-image:latest
          echo "Waiting for container to start..."
          sleep 10
          echo "Pinging the container..."
          curl --fail http://localhost:8080

      # Шаг 8: Вход в Docker Hub
      - name: Log in to Docker Hub
        if: github.ref == 'refs/heads/dev' && github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Шаг 9: Сборка и публикация образа
      - name: Build and push to Docker Hub
        if: github.ref == 'refs/heads/dev' && github.event_name != 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/rs-react-apps:latest # Публикуем образ в Docker Hub

  # --- НОВЫЙ JOB ДЛЯ ПУБЛИКАЦИИ НА GITHUB PAGES ---
  deploy:
    name: Deploy to GitHub Pages
    # Запускать только после успешного завершения тестов
    needs: build-and-test
    # Запускать на последней версии Ubuntu
    runs-on: ubuntu-latest

    # Задаем права для GITHUB_TOKEN, чтобы разрешить публикацию
    permissions:
      contents: read
      pages: write # Право на запись на GitHub Pages
      id-token: write # Право на использование OIDC токена
    # Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
    # However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
    concurrency:
      group: 'pages'
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        # Эта команда создаст папку 'dist'
        run: npm run build -- --base=/${{ github.event.repository.name }}/

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload production build as artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Загружаем содержимое папки, созданной на шаге 'build'
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      # Смок-тестирование: проверяем, что страница доступна и содержит нужный текст
      - name: Verify deployment
        run: |
          echo "Verifying deployment at: ${{ steps.deployment.outputs.page_url }}"
          # Даем GitHub Pages немного времени на "разогрев"
          sleep 10
          # Используем curl для скачивания и grep для поиска текста.
          # Флаг --fail заставит curl упасть, если статус не 200 OK.
          # Пайп | передает вывод curl на вход grep.
          # Флаг -q (quiet) для grep означает "не выводи найденную строку, просто завершись с нужным кодом".
          curl -s --fail "${{ steps.deployment.outputs.page_url }}" | grep -q "Top controls"
